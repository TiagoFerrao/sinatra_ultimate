source 'https://rubygems.org'

# Commented-out items should be uncommented out when running in Linux; they don't
# work in (at least) some Windows configurations.

gem 'ruby'
gem 'rspec-core'
gem 'rspec-expectations'
gem 'rspec-mocks'
gem 'sinatra'
# gem 'dropbox-sdk-v2'
gem 'rake'

group :test do
  gem 'minitest', '5.9.0'
  gem 'rack-test'
  # gem 'simplecov', :require => false, :group => :test
end
